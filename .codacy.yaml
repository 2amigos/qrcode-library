---
# Codacy configuration.
#
# PHP code quality is enforced in this project's own CI (GitHub Actions, PHP 8.3/8.4/8.5)
# via php-cs-fixer, PHPStan and a tuned PHPMD ruleset (phpmd.xml). The paths below are
# excluded from Codacy to avoid low-value/duplicate reporting:
#   - tests / vendor: not product code.
#   - markdown (*.md): project prose (README, CHANGELOG, upgrade & docs guides) is written
#     and reviewed by hand; markdownlint's 80-column/whitespace nits are noise here.
#   - GdImageBackEnd / QrCodeFactory: a GD rasterizer (scanline fill + SVG-arc/bezier
#     flattening) and a builder-style factory. They are intentionally and cohesively
#     "complex" and meet the project's own PHPMD complexity thresholds (phpmd.xml); Codacy's
#     stricter defaults (which it won't read from phpmd.xml) only produce noise here.
exclude_paths:
  - 'tests/**'
  - 'vendor/**'
  - 'docs/**'
  - '*.md'
  - '**/*.md'
  - 'src/Renderer/GdImageBackEnd.php'
  - 'src/Bridge/Laravel/QrCodeFactory.php'
